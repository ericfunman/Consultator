# Tests Configuration - Version stable et minimale
# Versions fixes pour éviter les conflits

# Tests de base (versions stables)
pytest==7.4.4
pytest-cov==4.1.0
pytest-html==4.1.1
pytest-mock==3.12.0
pytest-xdist==3.5.0  # Parallélisation des tests pour réduire temps CI/CD

# Outils de qualité (optionnels en CI)
# pylint==3.0.3
# flake8==7.0.0
# bandit==1.7.5

# Utilitaires de test
requests-mock==1.11.0
